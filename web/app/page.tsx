// A főoldal egy kártya-rácsos hub, a user szerepkörétől függően más-más
// kártyákkal (shipper / carrier / kijelentkezett).
import HomeHub from '@/components/HomeHub';

export default function Home() {
  return <HomeHub />;
}
