node_modules/
.env
.env.local
dist/
build/
.expo/
.next/
*.log
.DS_Store
coverage/
uploads/
tsconfig.tsbuildinfo
# 2026-09-11 (user-döntés D6): a backend és a web lockfile-ja VERZIÓKÖVETETT —
# a registry-változás 2026-09-10-én eltörte a CI-t és a Railway-buildet; a
# lockfile + npm ci teszi a telepítést reprodukálhatóvá. A mobil (Phase 6)
# lockfile-ja egyelőre marad kívül.
mobile/package-lock.json

# Claude Code személyes beállítások
.claude/settings.local.json

# Beágyazott teszt-Postgres adatkönyvtára
# A párhuzamos teszt-futások portonként külön adatkönyvtárat kapnak
# (GOFUVAR_TEST_PG_PORT) — a minta mindet lefedi.
backend/tests/.pg-data*/

# Playwright E2E
web/e2e/.pg-data/
web/playwright-report/
web/test-results/
.vercel
.env*

# AI felderítő tesztelő kimenete (web/scripts/ai-tesztelo.mjs) — munkatermék,
# minden futás felülírja; a valós találatokból determinisztikus teszt lesz
ai-teszt-jelentes.md
web/ai-teszt-jelentes.md

# Lefedettségi riport (minden futás újragenerálja)
coverage/

# Mutációs tesztelés (Stryker) — ideiglenes másolat és riport
.stryker-tmp/
backend/reports/

# Claude Code session-állományok (worktree-k, futásidejű state)
.claude/worktrees/
scratch/
